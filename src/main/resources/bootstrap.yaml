spring:
  cloud:
    propertiesvolume:
      prefixed: false
      paths: /mnt/secrets/rd
      aliases:
        location-ref-api-POSTGRES-PASS: spring.datasource.password
        ApplicationInsightsInstrumentationKey: azure.application-insights.instrumentation-key
        rd-location-storage-account-name: RD_LOCATION_STORAGE_ACCOUNT_NAME
        rd-location-storage-account-primary-key: RD_LOCATION_STORAGE_ACCOUNT_PRIMARY_KEY
        CONTAINER-NAME: CONTAINER_NAME
